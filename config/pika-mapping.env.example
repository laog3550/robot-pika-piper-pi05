# 左右 Pika 映射的现场记录。复制为 Git 忽略的 config/pika-mapping.env 后填写。
#
# 为什么需要它：mapping_order 只决定“pika_L_code 和 pika_R_code 哪个算左”，
# 而哪只手柄的 code 存在哪个变量里是机器现场事实。取值错误时症状是静默的——
# 两侧位姿照样 120 Hz、localization_status 照样 accurate，但左右对调。
#
# 确认方法（依据 IMU 与位姿必须同侧响应，不依赖记忆）：
#   1. 启动 input-only（可先用任意取值）
#   2. 采样窗口内只移动一只手柄：scripts/check_pika_mapping.py --duration 10
#   3. 换另一只手柄再测一次
#   4. 把结论写在这里，之后每次启动前 source 本文件
#
# 只有两个合法取值：
#   direct  = pika_L_code 那只手柄驱动 left、pika_R_code 那只驱动 right
#   swapped = 反过来
#
# 本文件含机器相关结论，不要提交到仓库。
PI05_PIKA_MAPPING_ORDER=
