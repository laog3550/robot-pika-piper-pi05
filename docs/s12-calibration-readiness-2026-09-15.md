# S12 真实标定前只读检查与回放模型修正

日期：2026-09-15。状态：阻塞（右侧反馈与现场安装信息待确认）。验证等级：只读真机观察、仿真/回放。没有使能、移动、模式切换或参数写入。

## 模型路径问题

原环境按 Noetic、参考 Pika install、锁定上游 devel、项目 devel 叠加后，`rospack` 实际解析的 `piper_description` 来自 `/home/mips/pika_ros/install/share/piper_description`。该旧模型并非锁定官方模型：

| 模型 | URDF SHA256 |
|---|---|
| 旧 install | `edb757a55e4ee795bbded680311a482c97f636cdb4b060e84e010cedacc271e8` |
| 官方锁定源 | `c1a5c29ae9b91160557ed02bddd47365a71f0eb1653abb4997eb34f124021486` |

J2 origin.rpy 从旧模型 `1.5708 -0.10095 -3.1416` 到官方 `1.5708 -0.1359 -3.1416`；J3 从 `0 0 -1.759` 到 `0 0 -1.7939`。两项各差约 2°；还有惯量及固定根 link 等差异。

这是后续 FK/IK/遥操模型选择的确定问题，不能解释此前完全未运行 IK 的原始 J2 运动测试。此前接线报告把模型来源写为官方锁定模型不准确，已更正并保留初次回放的实际来源说明。

## 修正与验证

仅修改离线回放：`tests/test_teleop_safety_pipeline.sh` 在自身 shell 中优先加入锁定官方 `piper_description` 这一个包目录，未加入整个历史上游工作区；`replay_component.py` 在启动 FK/IK/遥操之前检查官方 URDF SHA256。不修改其它终端环境或生产控制节点。

- `bash tests/test_teleop_safety_pipeline.sh`：官方模型下完整 FK→遥操→IK→过滤器→协调器回放 PASS，退出码 0。
- 显式选择旧 install 模型调用受保护 FK 入口：在启动数值/ROS 组件前拒绝，负向检查 PASS。
- 关闭独立 11333 回放 Master；真机 Pika input-only 观察保留。

## 干净只读观察

首次临时探针取出统计字段时仍有订阅回调运行，出现 KeyError，不能用其总退出码作为验收通过。该探针已退出；重新采集时在锁下停止接收并生成独立快照，注销订阅，再做 CAN/FK 检查；重新运行无回调错误，退出码 0。没有修改生产输入转发器。

5 秒 Pika 观察：左 pose 600 条，全部有限、四元数单位长度检查通过、坐标系单一非空；状态 735/735 有效。右 pose 599 条同样满足数值/四元数/坐标系检查，但状态 0/5 有效，因此不能将持续发布 pose 表述为右侧跟踪有效。

左 Piper 3 秒被动流检查收到 9,121 帧，20/20 类反馈齐全、没有控制/配置帧。后续独立 2 秒窗口获取完整关节组，六轴各 80 个 disabled 样本、零 enabled 样本；以官方模型计算 FK，结果数值有限，但未导出绝对位姿，也不构成真实标定通过。

右 Piper 3 秒流检查零帧，后续 2 秒窗口也没有完整反馈或失能状态样本。未启动任何驱动、未重新配置 CAN、未尝试恢复或使能。现场需确认右侧是否刻意断电/未接 CAN，不能直接认定设备故障。

## 下一步所需事实

右 Piper 当前供电及 CAN 连接状态；两台 Piper 底座实际安装方向；末端为原装夹爪还是加装工具/转接件。上游 0.19 m 末端偏移尚未经过现场测量，不直接当作真实标定值。仅有静置 Pika pose 和 FK 结果不能自动确定设备与机械臂坐标关系；还需带已知方向的输入动作和独立参考测量。

左臂 J3/J5 非目标响应继续保持未闭环；支撑干涉只是现场待核实假设。本次不进行真机遥操，不提交、推送或发布 PR。记录不含设备标识、原始位姿或 CAN 载荷。
