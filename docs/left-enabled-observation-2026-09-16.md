# 左臂已使能状态观察（2026-09-16）

用户要求在 Pika 充电期间测试机械臂使能，并确认两臂周围清空、支撑不阻碍关节运动、
现场可立即使用急停，选择先测试左臂。

## 执行与结果

测试前双侧 CAN 各 5 秒六轴关节反馈范围为零。使用固定 SDK
`C_PiperInterface_V2('left_piper')`、`ConnectPort(piper_init=False)` 连接，
关闭初始化查询；同时用独立 SocketCAN 接收核验电机状态及关节反馈。
未启动 ROS 驱动、遥操作、IK 或新的 Pika 节点。

2 秒基准采样发现左侧六电机已全部 enabled，关节反馈静止，因此没有重复使能。
随后的 10 秒观察结果：

| 项目 | 结果 |
|---|---|
| 六电机结束状态 | 全部 enabled |
| 每轴新鲜低速状态样本 | 各 400 个 |
| 六轴相对基准最大绝对变化 | 全部 0.000000 rad |
| 观察到的控制／配置帧 | 无 |
| 本次发送使能命令 | 0 次 |
| 本次发送关节目标、模式、复位、夹爪或停止指令 | 0 次 |

测试程序退出并关闭自身 CAN 连接。未改变现有 ROS 节点，未操作右臂。
左臂仍保持使能，没有自动失能。

## 限制

这次只验证已使能状态下的静止反馈；没有发生失能到使能切换，不能证明使能瞬间
无运动，也不能替代外部物理运动观察、带目标的运动测试或遥操作验收。
历史非目标关节响应问题未据此判定解决。Pika 定位仍需充电结束后复查。
