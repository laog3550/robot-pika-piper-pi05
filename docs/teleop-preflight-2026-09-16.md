# 直接遥操作上机检查（2026-09-16）

本次仅执行现有 ROS 查询、CAN 被动接收与 Pika 订阅，没有启动或停止既有节点，
没有发送 CAN 指令、调用使能服务或执行运动。

## 结果

| 检查 | 结果 |
|---|---|
| 左右 CAN 身份、UP、1 Mbps | 两侧通过 |
| 左 CAN 5 秒被动反馈 | 15,201 帧，20/20 反馈 ID，无控制／配置 ID |
| 右 CAN 5 秒被动反馈 | 15,201 帧，20/20 反馈 ID，无控制／配置 ID |
| ROS 当前节点 | 双 Pika input-only 定位／转发及双侧 Piper 只读反馈已运行 |
| 左 Pika 10 秒 | 1,202 位姿，119.9 Hz，10 状态，0 accurate |
| 右 Pika 10 秒 | 1,201 位姿，119.8 Hz，10 状态，0 accurate |

Pika 输入流存在，但两侧定位有效性检查失败。频率通过不能证明定位可用于遥操作。
未检测到设备活动不作为本次失败依据，因为没有要求现场移动 Pika。

## 诊断修复

首次检查订阅旧厂商话题，收到零样本；当前 locator 已将这些话题重映射至
input-only 路径，因此该次结果不能作为设备无数据的证据。
诊断工具现默认订阅 `/pi05/pika_input/{left,right}/pose` 和
`localization_status`，保留 `--topic-layout vendor` 检查旧入口。
话题选择及既有聚合判断回归测试通过。

## 后续

由现场恢复两只 Pika 的定位条件，再重复 10 秒检查。两侧 accurate 状态通过后，
继续分侧遥操作联调。当前没有双臂遥操作成功或历史非目标响应已解决的证据。
